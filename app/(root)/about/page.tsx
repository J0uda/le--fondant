import HeroImageWithTitle from '@/components/shared/heroImageWithTitle';

const AboutPage = () => {
  return (
    <main className='container'>
      <HeroImageWithTitle title={'about'} image={'/img/hero/contact.jpg'} />
    </main>
  );
};

export default AboutPage;
